"""Tax-invoice rendering, storage, and orchestration.

Commit #3 of `docs/prompts/INVOICES_TASK.md` lands the renderer; the
storage layer (commit #4), service orchestrator (commit #5), webhook
integration (commit #6), and HTTP / UI layers (commits #8+) are added
incrementally on top.
"""
